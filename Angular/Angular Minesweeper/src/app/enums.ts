export enum Result {
  WIN = 'You won! 🥳',
  GAME_OVER = 'Game Over! 💥',
  EMPTY = '',
}

export enum Sounds {
  FLAG = 'flag',
  EXPLOSION = 'explosion',
  SUCCESS = 'success',
}

export enum Emoji {
  FLAG = '🚩',
  BOMB = '💣',
  EMPTY = ''
}
